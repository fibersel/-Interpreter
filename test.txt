main
{
 struct c {
    int a;
 };
 c s;

 int a;
 ++a;
 --a;
 goto L;
 int b,i,k,j,heh;
 for(i = 0;i < 10;i++){
    for(j = 0;j < 10;j++){
        while(true){
            write("kek");
            read(heh);
            1 + 3 / 2;
            break;
        }
        not true;
        i = 3 + j++ + a = b;
        for(k = 0;k < 1;k++){
            write("heh");}
        break;
        if(b < i)
            write("heh");
        else if (b == i)
            write("lol");
        else
            read(a);
    }
    break;
 }
 while (i < j){
    write(a);
 }

 k = ( i + j ) * 2; /* ERROR – нет описания переменной i*/
 write ("ERROR not detected");
}